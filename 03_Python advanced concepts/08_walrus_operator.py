#it is newly assigned operator in lastest version of python 3.8
#it allows us to assign a value ti a variable as part of an expression
while(data:=input("Enter the vale:")):
    print(data)
    if data == "q":
        break